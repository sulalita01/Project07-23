import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Maimunah Japanese Crepe',
  webDir: 'www'
};

export default config;
